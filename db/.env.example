# ════════════════════════════════════════════════════════════════════════════
#  packages/database/.env — used by drizzle-kit scripts (push / migrate / seed
#  / studio) when run from this package directly.
#  In dev: local Postgres. In CI/prod: DO managed PG (sslmode=require).
# ════════════════════════════════════════════════════════════════════════════

DATABASE_URL=postgresql://milkia:milkia@localhost:5432/milkia
